import React from 'react';

const SummonerBox = props => {

  const { TFTData } = props;
  console.log(TFTData[0]);

  return (
    <div>
      {/* <p>Name: { summonerName }</p> */}
    </div>
  );
};

export default SummonerBox;
